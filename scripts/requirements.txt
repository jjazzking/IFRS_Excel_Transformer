olefile>=0.47
pymupdf>=1.24        # 의사록 PDF 본문·낱말 좌표 추출 (scripts/minutes_text.py)

# 스캔 페이지 OCR 은 시스템 패키지다 (pip 가 아니다).
#   apt-get install tesseract-ocr tesseract-ocr-kor
# 없어도 규칙 전용 경로는 그대로 돈다 — `--ocr` 를 안 주면 부르지 않는다.
